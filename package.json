{
  "dependencies": {
    "global": "^4.4.0",
    "nodeJS": "^1.0.0",
    "parcel-bundler": "^1.12.5",
    "redux": "^4.0.5",
    "redux-actions": "^2.6.5",
    "redux-devtools-extension": "^2.13.9"
  },
  "scripts": {
    "predeploy": "rm -rf dist && parcel build index.html --public-url 'https://dlwlsdn201.github.io/basketball-game/' && cp CNAME ./dist",
    "deploy": "gh-pages -d dist",
    "start": "parcel index.html"
  },
  "devDependencies": {
    "shx": "^0.3.3",
    "parcel": "latest"
  }
}
